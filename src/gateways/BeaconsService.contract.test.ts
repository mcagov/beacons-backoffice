describe("The remote Beacons service", () => {
  xit("Complies with the contract", () => {
    // TODO: Call the live beacons service in staging or prod (?) and assert that the return value meets the
    // contract expected by BeaconsGateway
  });
});
