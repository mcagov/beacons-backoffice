import { BeaconsGateway } from "./BeaconsGateway";

describe("BeaconsGateway", () => {
  const BEACONS_SERVICE_URL = "http://beacons.com"; // TODO: Update

  it("queries the correct endpoint", () => {
    // TODO: mock axios and assert it is called with correct endpoint when known
  });

  it("handles the returned beacons", () => {});
});
