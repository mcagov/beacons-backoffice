describe("emergencyContactOperations", () => {
  describe("getEmergencyContacts()", () => {
    xit("retrieves emergency contacts by beacon ID", () => {});

    xit("handles unrecognized ids", () => {});
  });
});
