import { BeaconOperations } from "./BeaconOperations";

describe("beaconOperations", () => {
  describe("getBeacon()", () => {
    xit("retrieves a beacon by its id", () => {
      const beaconOperations = new BeaconOperations();
    });

    xit("handles unrecognized ids", () => {});
  });
});
